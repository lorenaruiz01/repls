function Sum(val1, val2) {
    return val1 + val2;
};

let result = Sum(5,20); // returns 25
console.log(result);

// or 
// document.write(result);

// Here, a function named sum adds val1 and val2 and returns the result. When you execute the function by calling it, it will return the value of adding whatever arguments you’ve chosen for val1 and val2.

